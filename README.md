Professional Python file organizer that automatically backup files by extension, creates backups, handles duplicate filenames, generates CSV reports, and logs operations. Built using modular OOP design, composition, pathlib, JSON configuration, and exception handling.
